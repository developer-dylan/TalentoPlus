services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: TalentoDB
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Qwe.123*
      PGDATA: /var/lib/postgresql/data
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

  talentoplus.web:
    image: talentoplus-web
    build:
      context: .
      dockerfile: TalentoPlus.Web/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=TalentoDB;Username=postgres;Password=Qwe.123*
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://0.0.0.0:79
      - Gemini__ApiKey=AIzaSyCR_F-V3Whhor5Rs8g4aE55dV02TIZqbig
      - Gemini__Model=gemini-2.5-flash
    ports:
      - "5079:79"
    depends_on:
      - db

  talentoplus.api:
    image: talentoplus-api
    build:
      context: .
      dockerfile: TalentoPlus.Api/Dockerfile
    environment:
      - ConnectionStrings__DefaultConnection=Host=db;Port=5432;Database=TalentoDB;Username=postgres;Password=Qwe.123*
      - Jwt__Secret=SuperSecretKey1234567890_ChangeMeInProd
      - Smtp__Host=smtp.gmail.com
      - Smtp__Port=587
      - Smtp__User=tu-correo@gmail.com
      - Smtp__Password=tu-app-password-aqui
      - ASPNETCORE_URLS=http://0.0.0.0:80
    ports:
      - "5001:80"
    depends_on:
      - db

volumes:
  postgres_data:
